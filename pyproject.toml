[project]
name = "alfred-mcp"
version = "1.0.0"
description = "Alfred — Nordic Due Diligence MCP Server"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.0",
]

[project.urls]
Homepage = "https://aidatanorge.no"
Repository = "https://github.com/AIDataNordic/alfred-mcp"
